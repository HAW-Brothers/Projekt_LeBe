<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:background="@drawable/store_list_item"
    >


    <ImageView
        android:id="@+id/market_logo"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"


        app:srcCompat="@drawable/common_plus_signin_btn_icon_light_normal" />
    <TextView

        android:layout_width="match_parent"
        android:layout_height="26dip"
        android:id="@+id/market_beschreibung"
        android:layout_toRightOf="@+id/market_logo"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_above="@+id/market_Name"
        android:textSize="12sp"
        android:ellipsize="marquee"
        android:text="Beschreibung"
        />
    <TextView
        android:id="@+id/market_Name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toRightOf="@id/market_logo"
        android:textSize="20sp"
        android:gravity="center_vertical"
        android:text="Name"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_above="@+id/market_beschreibung"

        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:text="download"
        android:id="@+id/market_download" />


</RelativeLayout>
